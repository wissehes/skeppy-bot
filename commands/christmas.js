exports.run = (client, message, args) => {
    if (args[0] === `christmas`){
        var serverInvite = "https://discord.gg/zkyNyfb";
        message.channel.send(`The invite link for the Trio Christmas Server is ${inviteLink}`)
    }
}